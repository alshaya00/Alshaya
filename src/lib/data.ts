/**
 * Re-export types from types.ts for backward compatibility
 * All data is fetched exclusively from PostgreSQL database
 * 
 * DEPRECATED: Import directly from './types' instead
 */

export type { FamilyMember } from './types';
